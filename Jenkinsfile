pipeline {
          agent {
                 'Dockerfile true'
   }

          stages {
                  stage('Build') {
                          steps {
                                  sh 'node --version'
                                  sh 'pwd'
         }
      }
   }
}
